{
  "type": "object",
  "additionalProperties": false,
  "required": ["live_hosts", "endpoints", "js_files", "params", "tech", "observations", "suggested_focus"],
  "properties": {
    "live_hosts": { "type": "array", "items": { "type": "string" }, "description": "Resolved, live in-scope hosts (https://host or host)." },
    "endpoints": { "type": "array", "items": { "type": "string" }, "description": "Discovered endpoints/URLs (full URLs preferred)." },
    "js_files": { "type": "array", "items": { "type": "string" }, "description": "URLs of JS bundles worth watching/mining." },
    "params": { "type": "array", "items": { "type": "string" }, "description": "Interesting parameters observed." },
    "tech": { "type": "array", "items": { "type": "string" }, "description": "Tech/fingerprints (frameworks, servers, WAF, cloud)." },
    "observations": { "type": "array", "items": { "type": "string" }, "description": "Notable facts (auth model, GraphQL, swagger, secrets in JS, etc.)." },
    "suggested_focus": { "type": "array", "items": { "type": "string" }, "description": "Areas/endpoints most worth hunting next, with the signal that makes them interesting." }
  }
}
